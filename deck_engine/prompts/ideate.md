Commander: $commander ($color_identity)
Archetype: $archetype
Selector's rationale: $rationale

Commander's ACTUAL printed ability (verified against Scryfall — base every claim on this text,
not on what you recall about the card from memory, which may be wrong or from a different
card entirely):
"""
$commander_oracle_text
"""

Bracket $bracket house rules:
- Game Changers: $game_changers
- Tutors: $tutors
- Two-card infinite combos: $combo_rule (backup wincon only, never the primary gameplan)
- Mass land destruction: $mld
$retry_note
Propose ONE distinct build angle/gameplan line for this commander+archetype pairing — a
specific way to lean into it (a particular subtheme, synergy package, or unusual angle other
pilots of this commander might not take). This is angle $angle_index of $angle_total being
explored in parallel; make yours genuinely different from the generic "obvious" build.

Ground this angle in the commander's specific mechanic above, not just its colors. Most of the
key cards/themes you name should actively enable, trigger, or pay off that mechanic — if a
generic same-colored commander could pilot this exact list just as well, the angle isn't
using what makes THIS commander distinct. A pile of good ramp/removal/draw in the right colors
is not a build angle.

You have web search available — use it if it helps you find a genuinely more interesting or
less-obvious synergy package for this commander than you'd come up with unassisted (what
experienced pilots are actually building, underused card interactions, etc.). But the
commander's oracle text above is the only authority on what the commander itself does — if
anything you find (or recall) conflicts with that text, the text wins, no exceptions. Every
synergy you describe must follow from the commander's actual printed text above — if the
archetype label suggests an ability the card doesn't actually have, build around what it
really does instead, not the label.
Respond only via the provided JSON schema.
