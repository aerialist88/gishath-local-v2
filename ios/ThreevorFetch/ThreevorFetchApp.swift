import SwiftUI

@main
struct ThreevorFetchApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
                .tint(Color(red: 0.75, green: 0.52, blue: 0.99)) // match the web UI's purple accent
        }
    }
}
